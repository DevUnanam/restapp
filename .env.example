# Django Settings
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (leave empty for SQLite in development)
DATABASE_URL=

# CORS Settings (comma-separated list of allowed origins)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Note: For production on Render, these will be set in the Render dashboard
# - SECRET_KEY will be auto-generated
# - DATABASE_URL will be automatically set from the database service
# - ALLOWED_HOSTS should include your Render domain (e.g., your-app.onrender.com)
# - DEBUG should be False
